[project]
name = "x4-obs"
version = "0.1.0"
description = "Agent telemetry and evidence receipts"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "ARIEX4Ops" }]
keywords = ["observability", "agents", "receipts", "x4"]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6", "mypy>=1.10"]

[project.scripts]
x4-obs = "x4_obs.cli:main"

[project.urls]
Homepage = "https://github.com/dhe-cruzer69/x4-obs"
Repository = "https://github.com/dhe-cruzer69/x4-obs"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
